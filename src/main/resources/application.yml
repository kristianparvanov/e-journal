spring:
  datasource:
    data-username: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ejournal?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

app:
  jwtSecret: JWTSuperSecretKey
  jwtExpirationInMs: 604800000
